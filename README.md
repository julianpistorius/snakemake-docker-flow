# Snakemake and Docker example

Shows how to:

- [x] Download remote data as a Snakemake action 
- [x] Automatically tag Docker images with custom 'hooks'
    - See the `hooks` directory
- [ ] Use Docker volumes to persist data between runs
 
